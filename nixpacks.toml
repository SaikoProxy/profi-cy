[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r api/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python api/server.py"

[variables]
NIXPACKS_NO_NODE = "1"
